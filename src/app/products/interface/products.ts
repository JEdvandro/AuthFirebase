export interface Products {
    name?: string;
    description?: string;
    price?: number;
    // tslint:disable-next-line: ban-types
    promotion?: Boolean;
    imgUrl?: string;
    filePath?: string;
}
